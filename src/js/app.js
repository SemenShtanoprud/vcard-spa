var vcard = angular.module('vcard', []);

vcard.controller('baseController', function ($scope) {
	$scope.countryName = 'Ukraine';
});

vcard.controller('contactController', function ($scope) {
	// section-contact
});

vcard.controller('aboutController', function ($scope) {
	// section-about
});